# T0.16 — Release-path test inventory

## Command under review
### Current script and duplicate check
- `test:release-path` in root `package.json` is currently:
  - `node --test test/release-package-candidate.mjs test/package-candidate-verifier.mjs test/release-ci-candidate-binding.mjs`
- Duplicate check on `.mjs` entries is clean; each referenced file appears exactly once.

### Git-backed inventory state
All five files below are tracked in git and unmodified in `git status` output at the time of this inventory.

| File | Tracked state (git) | Current inclusion in command | Behavior and requiredness for release-path truth |
|---|---|---|---|
| `test/release-package-candidate.mjs` | tracked (`100644`, blob `84451646c0116ddd2e5198358ce1b70f7554adec`) | included | Required: validates the release wrapper argument parsing, immutable artifact staging, verifier/rollback coupling, evidence receipts, and npm publish guardrails through direct imports from `scripts/release-package-candidate.mjs`. |
| `test/package-candidate-verifier.mjs` | tracked (`100644`, blob `146999322de63b2a9ea91a2fffae2dd6a564abc2`) | included | Required: validates candidate archive structure, allowlist enforcement, private artifact leakage prevention, lifecycle script constraints, and consumer-install safety by invoking `scripts/verify-package-candidate.mjs` and `scripts/test-consumer-install.mjs`. |
| `test/release-ci-candidate-binding.mjs` | tracked (`100644`, blob `5922560aee15d721c2dddf2fc4af04c4ff452980`) | included | Required: enforces CI candidate tarball binding semantics in `.github/workflows/ci.yml` and verifies all candidate consumers use `SKILLMAP_TEST_TARBALL` from verified output. |
| `test/release-operations-ci.mjs` | tracked (`100644`, blob `782c734f2b24cf27ca840b3de2259c0da989fed8`) | omitted | Required for strict release-path truth: validates dual-authority CI preflight sequencing, pinned action immutability, Node matrix control, and evidence artifact retention gates that gate candidate legitimacy. |
| `test/release-truth-consistency.mjs` | tracked (`100644`, blob `bf0d3ca8b30b340561832672769a1c93bc18e71a`) | omitted | Required for full release truth: binds the canonical candidate, merge, tree, CI receipts, ledgers, and launch boundaries, and prevents source acceptance from being misreported as deployment or public launch. |

### Exact required set and omitted-file rationale
- Included now: `test/release-package-candidate.mjs`, `test/package-candidate-verifier.mjs`, `test/release-ci-candidate-binding.mjs`.
- Omitted now: `test/release-operations-ci.mjs` (required release-path policy layer) and `test/release-truth-consistency.mjs` (required release/launch truth layer).
- The current command is incomplete for the plan's full candidate/release truth boundary because both omitted tests protect release integrity beyond the package-path mechanics already covered.

## Minimal `package.json` correction candidate for T0.17
- Minimal command update, preserving existing structure and adding each discovered release test file once:
  - `"test:release-path": "node --test test/release-package-candidate.mjs test/package-candidate-verifier.mjs test/release-ci-candidate-binding.mjs test/release-operations-ci.mjs test/release-truth-consistency.mjs"`

## Completeness conclusion
- PASS/FAIL: **FAIL** for the current command because it does not execute all tracked release-path candidate files.
- If `test:release-path` is intended as a strict release integrity command, the above T0.17 correction is a PASS-state path because all tracked release* files and the verifier file are then covered exactly once with no command duplicates.
