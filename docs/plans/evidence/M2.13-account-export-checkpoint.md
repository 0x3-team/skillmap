# M2.13 — Account export checkpoint

Status: **APPROVED_LOCAL_ONLY**
Accepted: 2026-08-05
Branch / HEAD inspected: `main` / `cb062dd35de76cc3b40c1d004a13b50663cc1aa6`

## Verdict

M2.13 is accepted for local-only scope. The final candidate extends the existing authenticated account-export route with one owner-derived managed Skill Vault export, preserves the existing response contract and no-store boundary, returns all ten managed sections through public identifiers, rejects foreign or inactive callers, excludes private coordinates and secret-bearing fields, and enforces both row and byte bounds.

No P1 or P2 finding remains. No commit, push, deployment, live/provider action, production credential action, or owner-pilot action was performed. The web route was validated by its repository-standard static server-route contract test plus TypeScript, ESLint, and the existing hosted-boundary suite; no authenticated browser or live export is claimed.

## Final implementation

- `supabase/migrations/20260727061200_skill_vault_account_export.sql`
  - adds the no-argument `api.export_my_managed_skill_vault()` owner export;
  - derives identity only from `auth.uid()` and rejects missing, deleted, or currently banned accounts;
  - projects ten deterministically ordered sections: managed skills, versions, releases, files, devices, import sessions, import receipts, route decisions, route selections, and corrections;
  - uses public relationship identifiers and excludes internal UUIDs, account/device/session/decision coordinates, credential and verifier material, manifest bodies/digests, storage keys, raw prompt/body/context, cleanup internals, and worker fields;
  - exposes explicit per-section `count`, `total`, `truncated`, and `items` metadata;
  - adds stable public-ID tie-breaks and enforces a hard 1 MiB serialized RPC boundary in addition to row caps;
  - grants execute only to `authenticated`; anon, service role, the vault definer, and PUBLIC remain denied.
- `supabase/tests/skill_vault_account_export.test.sql`
  - exact 75-assertion pgTAP plan, `finish()`, and rollback;
  - covers function/grant posture, missing/banned/deleted callers, two-account isolation, all ten sections, public relationship projection, deterministic repeat and digest, row-cap metadata, redaction canaries, and zero fixture residue;
  - proves the hard byte cap with a variable-width 1.2 MB fixture that fails closed at the RPC boundary.
- `apps/web/app/account/export/route.ts`
  - preserves the existing authenticated owner gate and existing profile/saved/submission/report fields;
  - calls the no-argument managed export RPC and includes it under stable `managedSkillVault` output;
  - validates the exact versioned ten-section envelope, section counts/totals/truncation/items, plain-object items, and the 1 MiB managed-vault bound before responding;
  - maps RPC or shape failure to the existing opaque safe error and retains the overall 2 MiB response cap and private no-store headers.
- `apps/web/tests/skill-vault-export.test.mjs`
  - seven focused repository-standard static source-contract checks for authentication, no account-id input, no-argument RPC use, exact section validation, byte bounds, safe errors, redaction posture, stable output field, and no-store headers.

## Independent acceptance evidence

All decisive commands ran against the final clean local candidate.

- Clean local reset: **PASS**; every migration through `20260727061200_skill_vault_account_export.sql` applied.
- M2.13 focused pgTAP: **PASS, 75/75**, exact plan, every assertion `ok`, `finish()`, and rollback.
- M2.13 route contract: **PASS, 7/7**.
- Existing hosted-boundary suite: **PASS, 42/42**.
- M2.12 route-RLS regression: **PASS, 65/65**, exact plan, `finish()`, and rollback.
- M2.11 device/import-RLS regression: **PASS, 68/68**, exact plan, `finish()`, and rollback.
- M2.10 managed-RLS regression: **PASS, 132/132**, exact plan, `finish()`, and rollback.
- M2.09 route-record regression: **PASS, 74/74**, exact plan, `finish()`, and rollback.
- Database lint at warning level: **PASS**, zero findings.
- Web TypeScript check: **PASS**.
- Scoped route/test ESLint: **PASS**.
- Scoped `git diff --check`: **PASS**.
- Focused fixture residue after rollback: all ten managed-vault tables contain `0` rows.

Effective export-function privilege probe after the final reset:

- `authenticated` execute: `true`.
- anon execute: `false`.
- `service_role` execute: `false`.
- PUBLIC execute: `false`.

## Final hashes

- migration SHA-256: `ca84e22df4a03c42dc2b716816770cbd68338e6cd11516713044d646f9e55086`
- focused pgTAP SHA-256: `2b3b67a03e039cfb9431d2378746707b9905f2c90425b7001dfcd2e7215c3e95`
- export route SHA-256: `079e8c984d28d1c62b73905f868ed0ee73337db8e0e3d8296a2b3f93585bb4eb`
- route contract test SHA-256: `615b8ca6f5e47f452f504a45af209fcb8719d3b428c31d5d7c3d08e60809b65f`

## Visible DeepSeek and coordinator receipt

Each visible Claude Code pane showed `DeepSeek V4 Flash 0731 · deepseek-v4-flash-0731`.

- Initial combined pane `term_eb123f57` wrote no file within the five-minute inactivity cap and was closed.
- SQL pane `term_c7d12966` produced the migration and iterated against a clean local reset.
- pgTAP pane `term_2344d418` produced the focused database test and iterated against the local database.
- Web pane `term_aad75c59` produced the route integration and focused route contract test.
- The coordinator independently audited the candidate, added stable public-ID tie-breaks, enforced a hard SQL byte boundary, added an executable oversized fixture, tightened the route's exact section validator, and ran the final clean validation matrix.

The strict required Luna High implementation identity was unavailable. The strict phase route therefore remains blocked with the user's previously authorized visible-DeepSeek/direct-repair exception; no claim is made that Luna High ran.

## Efficiency retrospective

- Splitting SQL, pgTAP, and web work into file-owned panes produced useful writes faster than one all-in-one context.
- The five-minute cap was useful only for no-write inactivity; productive file iteration was allowed to continue.
- Independent review focused on three executable risks: complete owner-only projection, deterministic/bounded serialization, and route compatibility. It did not repeat unrelated historical suites.
- For M2.14, split the first pass into deletion SQL and destructive pgTAP ownership, then add application compatibility only if the exact plan requires it. Require the migration skeleton first and validate deletion order against the real foreign-key/storage-job graph before broad tests.

## Phase state

- M2.13: **current and APPROVED_LOCAL_ONLY**.
- M2.14: **eligible, not started**.
- M2.15 and later: **gated**.
- Commit/push/deploy/live/provider/credential/owner-pilot work: **not performed**.
