# M2.12 — Route RLS checkpoint

Status: **APPROVED_LOCAL_ONLY**
Accepted: 2026-08-05
Branch / HEAD inspected: `main` / `cb062dd35de76cc3b40c1d004a13b50663cc1aa6`

## Verdict

M2.12 is accepted for local-only scope. The final candidate adds the route decision, selection, and correction policy/grant boundary; separates connector adapters from authenticated dashboard history/correction; revalidates stored authority before replay/read/correction disclosure; preserves privacy-safe bounded surfaces; and passes the focused and affected predecessor suites from a clean local reset.

No commit, push, deployment, live/provider action, production credential action, or owner-pilot action was performed.

## Final implementation

- `supabase/migrations/20260727061100_skill_vault_route_rls.sql`
  - keeps FORCE RLS and zero application-role base-table grants on the three route tables;
  - adds exactly one inert authenticated owner-select policy and one NOLOGIN dashboard-definer select policy per table;
  - grants the dashboard definer only owner-scoped SELECT, with zero route-table writes;
  - exposes exactly three `route_adapter` connector functions to `service_role`, while `service_role` retains no `private` schema usage;
  - derives connector account/device authority through the M2.11 token context and derives dashboard ownership through `auth.uid()`;
  - adds an authenticated dashboard correction command that accepts no account or device UUID;
  - validates live account, device, decision expiry, active-release pointer, release lifecycle/revocation, and stored selection lineage before replay, read, correction, or dashboard visibility;
  - revalidates stored lineage even when an exact replay supplies no selection payload;
  - requires explicit UUID correction idempotency keys and returns the same bounded receipt on exact replay;
  - accepts and returns no raw prompt, body, context, embedding, provider response, token verifier, storage key, or internal account/device UUID through reachable route surfaces.
- `supabase/tests/skill_vault_route_rls.test.sql`
  - exact pgTAP plan plus `select * from finish()` and rollback;
  - 65 reachable-role and authority assertions covering policy/grant inventory, two accounts, multiple devices, anon, stale revision, expired token, banned account, foreign lineage, active-pointer mismatch, disabled stored release, expired decision, replay equivalence/conflict, connector and dashboard correction, direct-DML immutability, owner isolation, privacy canaries, and zero residue.
- Forward-test posture only:
  - `supabase/tests/skill_vault_route_records.test.sql` now expects the exact two M2.12 policies per route table.
  - No predecessor migration or predecessor evidence file was edited.

## Independent acceptance evidence

All decisive commands below ran against the final clean local candidate.

- Clean local reset: **PASS**; all migrations through `20260727061100_skill_vault_route_rls.sql` applied.
- Database lint at warning level: **PASS**, zero findings across `api`, `device_adapter`, `extensions`, `private`, `public`, and `route_adapter`.
- M2.12 focused pgTAP: **PASS, 65/65**, exact plan, every assertion `ok`, `finish()`, and rollback.
- M2.09 route-record regression: **PASS, 74/74**, exact plan, every assertion `ok`, `finish()`, and rollback.
- M2.10 managed-RLS regression: **PASS, 132/132**, exact plan, every assertion `ok`, `finish()`, and rollback.
- M2.11 device/import-RLS regression: **PASS, 68/68**, exact plan, every assertion `ok`, `finish()`, and rollback.
- Scoped `git diff --check`: **PASS**.

Effective privilege probe after the final reset:

- route-table policies: exactly `6`.
- `service_role` executable functions in `route_adapter`: exactly `3`.
- browser-executable functions in `route_adapter`: `0`.
- `service_role` `USAGE` on `private`: `false`.
- application-role direct route-table privileges: `0`.
- dashboard-definer route-table SELECT grants: exactly `3`.
- dashboard-definer route-table write grants: `0`.

Focused fixture residue after rollback:

- route decisions: `0`
- route decision selections: `0`
- route corrections: `0`

## Final hashes

- migration SHA-256: `b8d510088561858c3c312833fbace55fb156258a1eac2cd1fab5f9264119a86e`
- focused test SHA-256: `a28f9592378bdfa32dda30b881d4e83a7b6dbc71c73e163db6e57fa4de43e635`
- M2.09 posture test SHA-256: `7d3b35f60f5cb635c2727f4220ad0eb3d682fdb3e86b46ed1b68dae80d3406b0`

## Visible DeepSeek and coordinator receipt

The visible Claude Code terminal showed `DeepSeek V4 Flash 0731 · deepseek-v4-flash-0731`.

- Initial pane `term_826df8c8-ce16-4917-b8bd-5e8d08f17656` produced the migration and focused test in one dispatch. It reached its context limit with six direct-DML message mismatches and a real dashboard-definer permission failure; it did not claim a clean result.
- Focused correction pane `term_e2fe02e5-3366-44d3-a972-571d549c09e1` received the independent rejection findings, but wrote no file within the five-minute inactivity cap and was closed.
- The coordinator then used the user-authorized direct-fix path to repair the runtime definer boundary, add the missing dashboard command, enforce stored-authority revalidation and explicit correction idempotency, expand adversarial tests, and complete independent validation.

The strict required Luna High implementation identity was unavailable. The strict phase route therefore remains blocked with the user's previously authorized visible-DeepSeek/direct-repair exception; no claim is made that Luna High ran.

## Efficiency retrospective

- The migration-first first-write cap worked: the initial DeepSeek pane produced useful files quickly, so productive iteration was allowed to continue.
- The second pane demonstrated the complementary rule: five minutes with no write is enough to stop a focused correction attempt.
- The independent audit stayed bounded to executable contract gaps and affected suites instead of repeating the full historical matrix.
- For M2.13, dispatch one narrow file-first deliverable, require an early test skeleton, and reserve Codex review for export completeness, redaction, bounds, and compatibility failures proven by the diff or tests.

## Phase state

- M2.12: **current and APPROVED_LOCAL_ONLY**.
- M2.13: **eligible, not started**.
- M2.14 and later: **gated**.
- Commit/push/deploy/live/provider/credential/owner-pilot work: **not performed**.
