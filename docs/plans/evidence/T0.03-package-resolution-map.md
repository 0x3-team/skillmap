# T0.03 — Package-resolution entry-point map

**Plan key:** `T0.03`
**Task ID:** `task_60710bddb27b`
**Dispatch ID:** `ctx_fe7c78273565`
**Timestamp:** `2026-07-23T18:53:18Z`
**Evidence level:** `local`
**Task owner:** worker
**Scope status:** read-only inspection + one report write only

## Files read
- `docs/plans/2026-07-22-skillmap-comprehensive-audit-remediation-implementation-plan.md` (plan contract + `T0.03` row)
- `package.json`
- `package-lock.json`
- `scripts/run-root-tests.mjs`
- `test/core.mjs`
- `test/slice-b-identity.mjs`
- `test/slice-b-policy.mjs`
- `test/slice-b-dashboard.mjs`
- `test/slice-b-share.mjs`
- `test/phase1-routing-readiness.mjs`
- `test/mcp-contracts.mjs`
- `test/mcp-stdio.mjs`
- `test/phase3-release-convergence.mjs`
- `test/package-candidate-verifier.mjs` (archive integrity assertions only)

## Executed evidence commands and exits
- `rg -n ...` (plan/target scans): exit `0`
- `cat package.json`: exit `0`
- `cat package-lock.json`: exit `0`
- `sed -n ... package scripts and helper files`: exit `0`
- `nl -ba ...` on each `test/*.mjs` helper: exit `0`
- `rg -n "dist/cli\.js" test ...`: exit `0`
- `for p in ...; do test -f "$p"; ...; done`: exit `0` (all required files exist)

## 1) Which process installs dependencies (fact + inference boundary)
- Fact: package manager artifacts are npm-centric:
  - `package-lock.json` has `"lockfileVersion": 3` at line `4` and root package metadata under `packages[""]` (file: `package-lock.json:4`, `package-lock.json:7`).
  - `package.json` scripts invoke `npm` in multiple commands, including root test orchestration (`"npm run build && ..."`, e.g. `package.json:15`).
- Fact: none of the inspected `package.json`, `package-lock.json`, `scripts/run-root-tests.mjs`, or in-scope test helpers contains an explicit dependency-install command (for example, `npm ci`).
- Inference boundary: exact install command is outside this task’s scoped files and is explicitly deferred to T0.02.
- Inference: dependency installation ownership is npm process-boundary control, while the concrete command (`npm ci` vs `npm install`) remains explicit T0.02 evidence.

## 2) Root test launcher executable and path
- Root script path: `package.json:15` sets `"test": "npm run build && node scripts/run-root-tests.mjs"`.
- Child-process launcher: `scripts/run-root-tests.mjs:24-27` calls `spawnSync(process.execPath, testArguments, { cwd: repo, env: process.env, stdio: 'inherit' })`.
- `process.execPath` is the executable used, so the Node binary resolved by the current process runs the test harness.

## 3) Root test selection, concurrency, and environment
- Test file discovery:
  - Reads `test` directory (`readdirSync`) and filters `*.mjs` except `phase3-local-app-browser-fixture.mjs`, then sorts (`scripts/run-root-tests.mjs:9-12`).
- Concurrency:
  - `SKILLMAP_TEST_CONCURRENCY` is read from environment, trimmed, and defaulted to `'3'` (`scripts/run-root-tests.mjs:18`).
  - Invalid values throw unless positive integer (`scripts/run-root-tests.mjs:19-20`).
  - CLI receives `--test-concurrency=<value>` via `testArguments` (`scripts/run-root-tests.mjs:22-23`).
- Environment:
  - root launcher forwards `env: process.env` into child test runner (`scripts/run-root-tests.mjs:26`) and uses `stdio: 'inherit'` (`scripts/run-root-tests.mjs:27`).

## 4) dist/cli.js spawn map (files/functions + call shape)
Direct spawn helpers / call sites and their exact child-process API:

- `test/core.mjs`
  - `run(args, cwd)` uses `execFileSync(process.execPath, [cli, ...args], { cwd, encoding: 'utf8' })` with `cli = path.join(repo, 'dist/cli.js')` (`core.mjs:12-16`).
- `test/slice-b-identity.mjs`
  - `run(args, cwd)` same direct helper shape, no explicit `env` (`slice-b-identity.mjs:16-20`).
- `test/slice-b-policy.mjs`
  - `run(args, cwd)` same direct helper shape (`slice-b-policy.mjs:18-20`).
- `test/slice-b-dashboard.mjs`
  - `run(args, cwd)` uses `execFileSync(process.execPath, [cli, ...args], { cwd, encoding: 'utf8' })` (`slice-b-dashboard.mjs:12-14`).
- `test/slice-b-share.mjs`
  - `run(args, cwd)` sets `stdio: ['ignore', 'pipe', 'pipe']` on the exec wrapper (`slice-b-share.mjs:12-14`).
- `test/phase1-routing-readiness.mjs`
  - `run(args, cwd)` wraps `execFileSync(process.execPath, [cli, ...args], { cwd, encoding: 'utf8', maxBuffer: 32 * 1024 * 1024 })` (`phase1-routing-readiness.mjs:101-102`).
- `test/phase3-release-convergence.mjs`
  - `run(args, cwd)` same wrapper with large maxBuffer (`phase3-release-convergence.mjs:296-300`).
- `test/mcp-contracts.mjs`
  - Inline `execFileSync(process.execPath, [path.join(repo, 'dist', 'cli.js'), 'mcp', 'manifest', '--json'], { cwd: repo, encoding: 'utf8', stdio: ['ignore', 'pipe', 'inherit'] })` (`mcp-contracts.mjs:31-35`).
- `test/mcp-stdio.mjs`
  - `runCli(args, cwd, environment)` merges environment and uses `execFileSync(process.execPath, [cli, ...args], { cwd, encoding: 'utf8', env: { ...process.env, ...environment }, stdio: ['ignore', 'pipe', 'inherit'] })` (`mcp-stdio.mjs:256-262`).
  - `openSpawnedSession(cwd, suffix)` uses `StdioClientTransport({ command: process.execPath, args: [cli, 'mcp', 'serve'], cwd, stderr: 'pipe' })` (`mcp-stdio.mjs:265-271`).
  - direct spawn in the same file: `spawn(process.execPath, [cli, 'mcp', 'serve'], { cwd: repo, stdio: ['pipe', 'pipe', 'pipe'] })` (`mcp-stdio.mjs:219-222`).
- `test/package-candidate-verifier.mjs` is excluded from the spawn map:
  - references `dist/cli.js` as a fixture/package artifact (`package-candidate-verifier.mjs:307`, `package-candidate-verifier.mjs:332`, `package-candidate-verifier.mjs:346`) but does not launch it as a command target; process launches are for local verifier/npm helpers (`package-candidate-verifier.mjs:493-505`).

## 5) Direct wrapper vs direct spawn classification
- Wrapper helper path (most suite files): define `run` function(s) around `cli` and call through that helper in tests (`core`, `identity`, `policy`, `dashboard`, `share`, `routing-readiness`, `release-convergence`, `mcp-stdio:runCli`, `phase3`, etc.).
- Direct command shape only appears in the helper internals above and inline in `mcp-contracts`/`mcp-stdio`:
  - inline call in `mcp-contracts` (`mcp-contracts.mjs:31-35`)
  - `spawn` in `mcp-stdio.mjs:219-222`
  - transport-based child process in `mcp-stdio.mjs:265-271`.

## 6) Environment inheritance snapshot
- `scripts/run-root-tests.mjs` passes inherited environment to test runner (`scripts/run-root-tests.mjs:26`).
- Wrapped `run(...)` helpers in most files inherit `process.env` by omission (`core.mjs:15`, `slice-b-identity.mjs:19`, `slice-b-policy.mjs:19`, `slice-b-dashboard.mjs:13`, `phase1-routing-readiness.mjs:102`, `phase3-release-convergence.mjs:296`).
- `runCli` in `mcp-stdio` explicitly re-injects inherited env and merges overrides (`mcp-stdio.mjs:256-261`).
- `StdioClientTransport` in `mcp-stdio` uses `command: process.execPath` with explicit `args` array and cwd (`mcp-stdio.mjs:265-270`).

## Criteria status
- PASS — install boundary is explicit (npm-managed dependency resolution), and exact install command is out-of-scope and deferred to T0.02; this report does not infer `npm ci` or `npm install`.
- PASS — root test launcher and executable identified (`npm run build && node scripts/run-root-tests.mjs`; internal launcher uses `process.execPath`).
- PASS — test selection, `SKILLMAP_TEST_CONCURRENCY`, and inherited environment behavior identified.
- PASS — `dist/cli.js`-spawning test files and helper symbols named with line-level evidence.
- PASS — direct launch vs helper/transport wrappers distinguished with exact call sites.
- PASS — file names + commands + environment inheritance documented, no fix proposed.

## Risks / remaining work
- Exact dependency-install command (`npm ci` vs `npm install`) is not present in the mandated read set; this report intentionally avoids asserting a non-evidenced command.
