# M2.02 — Independent Luna High engineering acceptance review R1

**Review mode:** read-only independent acceptance review; no target artifact, migration, database, runtime, provider, credential, deployment, Git, owner-pilot, or duplicate-suffix owner-file action was performed.

**Verdict:** `PASS`

**Approval marker:** `APPROVED_INDEPENDENT_LUNA_M2_02_R1_PASS`

**P1 findings:** `0`
**P2 findings:** `0`

## Reviewed inputs

| Input | Result / SHA-256 |
| --- | --- |
| `docs/plans/evidence/M2.02-forward-only-skill-vault-migration-series.md` | Expected and observed `ece797b115bc0d1b8e7623341fc30bde75aa9ec8953cf3c72a5b5a7fd358da23` |
| `docs/plans/evidence/M2.02-import-vault-error-registry-v1.schema.json` | Expected and observed `9a9738dbbe64d8fff9ab5211b1ca7902bc89dfc6c79d7d3a0801206e123a6223` |
| `docs/plans/evidence/M2.02-import-vault-error-registry-v1.json` | Expected and observed `182fa493af69f8d506e5acae84f42445d28c939e716d3f3d5d106aad13d82f2e` |
| Accepted M1 packet | `M1.12-phase-contract-packet-approval-r11.md`, `1e5ba84b8bf4f501e2e8c9ff760a50983de9768b42432be52de6134a01e547ea`; marker `APPROVED_INDEPENDENT_LUNA_M1_PACKET_R11_PASS` |
| Accepted M1 adversarial sidecar | `M1.12-phase-contract-packet-adversarial-r11.md`, `f1e9ffedb2da294daaa0052e32070674240c149a62447d7b585cdc80b66de78f`; marker `APPROVED_ADVERSARIAL_LUNA_M1_PACKET_R11_PASS` |
| Accepted M2.01 inventory | Target `3574d9ce4e4a35bf9141c4586615f219f09691b5643a9a0ccb2994a35cf8c24b`; review `1009880e72b4752cf9df9985d0570995e8f832c0eb4b80d2a6ef9224fb5a1e33`; marker `APPROVED_INDEPENDENT_LUNA_M2_01_R3_PASS` |

The current branch snapshot was `main...origin/main [ahead 2, behind 4]` with the pre-existing dirty tracked worktree preserved. The three M2.02 targets were untracked and unchanged by this review. The protected duplicate-suffix owner files were not read, edited, staged, reverted, deleted, or otherwise touched.

## Acceptance checks

### 1. Phase gate and scope

PASS. The canonical plan’s current provenance correction records M1 as `CLOSED_APPROVED_FOR_M2`, M2.01 as independently accepted, and M2.02 as the only dispatched M2 task (`docs/plans/2026-07-27-skillmap-cloud-managed-skills-routing-implementation-plan.md:37-47`). The M2.02 target remains `PENDING_INDEPENDENT_LUNA_REVIEW` and explicitly claims architecture-only evidence, not implementation or approval.

PASS. The target preserves the safety boundary: no migration file, schema, code, configuration, test, runtime, remote database, provider, credential, deployment, Git, or owner-pilot action is claimed (`M2.02-forward-only-skill-vault-migration-series.md:1-20`). M2.03 remains gated until this review and registry reconciliation pass.

### 2. Migration baseline and ordering

PASS. Read-only inspection found exactly 15 existing SQL migrations, with latest file `20260727060100_reload_postgrest_schema_cache.sql`. The existing `supabase/migrations` path has an empty Git diff. No existing migration is edited or proposed for replacement.

PASS. The proposed series has exactly 12 unique, strictly increasing filenames, one each for M2.03 through M2.14, beginning at `20260727060200` and ending at `20260727061300` (`M2.02-forward-only-skill-vault-migration-series.md:110-128`). M2.15 creates no migration and follows M2.14; M2.16 is the coordinator-owned remote apply gate after M2.15 (`:338-375`).

PASS. M2.04’s internal order is explicit and safe: immutable versions, then release bindings and lifecycle eligibility, then the active release pointer with expected-revision CAS. The plan does not reuse catalog `private.skills.current_version_id` and keeps release lifecycle derived from the binding authority (`:147-171`; canonical success criteria `2026-07-27...implementation-plan.md:519-525`).

PASS. Every M2.03–M2.14 section was traced for owner/scope, objects or policy/function surface, prerequisites/order, constraints/indexes, RLS/grants/storage where applicable, transaction/lock behavior, backfill/compatibility, recovery/restore, and named future tests. Each phase has a focused `supabase db reset`, lint, and pgTAP command; M2.13/M2.14 additionally name the account export/deletion Node tests. No phase silently claims execution.

### 3. PostgreSQL, Supabase, and data-boundary rules

PASS. The frozen cross-series rules explicitly require:

- forced RLS on every account-owned canonical table, deny-by-default creation, and cached scalar `(select auth.uid())` owner predicates (`:75-83`);
- account-repeated composite foreign keys and same-account/same-parent binding checks (`:53-73`);
- checked text states, SHA-256 and HMAC digest grammars, positive key versions, nonnegative CAS revisions, scoped idempotency, server-owned timestamps, and immutable-coordinate enforcement (`:53-73`);
- policy/filter-support indexes, pagination indexes, live-state partial indexes, scoped replay indexes, uniqueness, and aggregate bound checks in the owning migration sections (`:133-336`);
- `skillmap_vault_definer` as `NOLOGIN`, fully qualified objects, `set search_path = ''`, explicit caller validation, revoke-first/allowlist grants, no blanket service-role private-table access, and security-invoker/barrier API projections (`:75-83`);
- a deterministic global lock order across account, managed-skill, version/release, file, device/token, import/receipt, route/selection/correction, deletion-receipt, and cleanup-job rows (`:84-100`); and
- no HTTP, storage, provider, KMS, queue, filesystem, or other external call while advisory or row locks are held (`:88-100`).

PASS. Forward recovery is correctly forward-only: transactional failure rolls back the current migration; post-commit defects require a new repair migration; no catalog backfill or migration-history edit is permitted; backup restore replays deletion barriers before reads; M1.11 owns retention, deletion, backup, and restore clocks (`:101-108`).

PASS. Catalog/vault separation is explicit. Existing public catalog relations and `private.skills`, `private.skill_versions`, `skl_`, `skv_`, and catalog `current_version_id` remain unchanged and are never joined with managed rows. Managed rows use separate `private.managed_*` authorities and `api.my_*`/verb-first owner projections; internal UUIDs, token digests, storage keys, raw prompts, raw bodies, and foreign-account existence are excluded from projections (`:15-51`). This aligns with accepted M2.01’s non-confusion and namespace decisions.

### 4. Per-step architecture and risk coverage

| Phase | Authority checked | Required focused acceptance named |
| --- | --- | --- |
| M2.03 | Managed Skill identity/aggregate only; no lifecycle, version, release, or pointer authority | identity ownership, prefix, bounds, projection, catalog non-confusion |
| M2.04 | immutable versions → release bindings → pointer/CAS | digest reuse, eligibility, foreign/stale/missing rejection, idempotency, concurrent CAS, immutability |
| M2.05 | bounded version file/blob references | path/traversal, duplicate path/order/key, digest/media/size, cross-account, aggregate bounds |
| M2.06 | private bucket and object binding | anon/two-account/service-role matrix, key grammar, type/size, signed expiry, cleanup idempotency |
| M2.07 | device identity and verifier-only HMAC token records | raw-token absence, vectors, expiry/revocation/replacement, rotation lineage, replay, cross-account |
| M2.08 | resumable import sessions and file receipts | legal/illegal transitions, replay conflict, exact totals/digest, expiry, crash/resume, terminal immutability |
| M2.09 | privacy-safe route decisions/selections/corrections | schema canary, isolation, selection bounds, correction uniqueness, replay/expiry, no raw prompt/body |
| M2.10 | managed RLS/grants and safe worker paths | 100% account/anon/service-role allow-deny matrix, direct immutable denial, catalog non-confusion |
| M2.11 | device/token/import RLS and functions | device context, scope/replay/expiry, enumeration denial, function-grant matrix, no lock-held HMAC/upload |
| M2.12 | route/correction RLS and functions | two-account/device, stale authority, replay equivalence/conflict, correction bounds, raw-data denial |
| M2.13 | bounded owner export extension | deterministic two-account digest, completeness, redaction, bounds, no-store compatibility |
| M2.14 | self-deletion barrier, relational purge, durable storage cleanup | crash/retry/idempotency, immediate inaccessibility, residue, receipt privacy, M1.11 barrier/restore proof |

PASS. The step order, ownership boundaries, locks, empty-start/backfill rules, compatibility seams, recovery paths, and focused test seams are coherently named for all twelve implementation slices. Export precedes deletion, deletion enqueues idempotent storage cleanup, and external object deletion occurs only after relational commit.

### 5. Versioned error-registry acceptance

Mechanical checks were run against the frozen schema and registry with the local Ajv 2020-12 validator:

| Check | Result |
| --- | --- |
| Schema parse | PASS |
| Registry parse | PASS |
| Registry validates against schema | PASS; zero Ajv errors |
| Public tuple closure | PASS; 5 tuples exactly match the schema’s five `oneOf`/`const` definitions |
| Entries | PASS; 28 total |
| Kind split | PASS; 9 public mappings, 14 delegates, 5 internal dispositions |
| Source diagnostics | PASS; 28 unique |
| Public codes | PASS; 5 unique, no unregistered code |
| One-field mutation closure | PASS; 5 tuples × 8 fields = 40/40 rejected |
| Registry status/version | PASS; `pending_independent_luna_review`, version `1`, closed-world rules |

The five frozen public tuples are `VAULT_RESOURCE_UNAVAILABLE`, `VAULT_STALE_REVISION`, `IMPORT_SESSION_EXPIRED`, `IMPORT_REPLAY_CONFLICT`, and `IMPORT_INFRASTRUCTURE_RETRYABLE`. Their disclosure, phase, terminal state, retryability, mutation, fresh-authorization requirement, next action, and bounded message all close under the schema. Non-enumeration is preserved for missing, foreign, RLS-denied, and unavailable resources.

PASS. Delegate ownership covers every accepted M1.03–M1.11 owner exactly once at the category boundary: manifest, import allow/deny, immutable digest/version, lifecycle, quarantine/restore, DeviceAuth, routing, Runtime Bundle, and export/retention/deletion/support. No M2.02 alias replaces an M1 code or retry rule. M1.04’s four existing import outcomes remain delegated; M1.07’s exact six-code quarantine registry remains authoritative; M1.08, M1.09, and M1.10 retain their closed authentication, routing, and bundle registries. M1.11 remains the sole owner for export, retention, deletion, support, backup/restore, and privacy clocks.

PASS. Duplicate digest is explicitly successful immutable-version reuse, not an error. SQLSTATE mappings preserve context: `23505` is internal context classification, `23503` is non-enumerating unavailable, input-invariant states delegate to M1.04, transient/deadlock/lock/timeout states use the bounded retryable tuple, storage-before-binding transient failure is retryable, storage binding mismatch is terminal internal disposition, unknown `P0001` is generic internal failure, and provider/table/constraint/path details remain internal redacted diagnostics. No raw SQLSTATE, Supabase, storage, provider, path, prompt, body, credential, or private identifier is public.

PASS. Versioning is closed and reserved: unknown source maps to `internal_unexpected_failure`; new codes require a new registry version and independent acceptance; aliases are forbidden; breaking changes require a new major ID. The target’s `REGISTRY_RECONCILED` line records the exact schema/registry hashes and `5/28/9/14/5/40` counts (`:377-390`).

### 6. Current validation truth and non-self-approval

PASS. M2.01’s current canonical `npm run test:contracts` failure is preserved truthfully as a pre-test `TS2688: Cannot find type definition file for 'node 3'` failure; the direct generator PASS, hosted-boundary 42/42 PASS, and historical direct 37/37 are not relabeled as a canonical PASS (`M2.01-hosted-schema-auth-inventory.md:232-253`). M2.02 carries that unresolved gate forward and requires separately authorized repair plus canonical rerun before M2.15/M2.16 (`M2.02-forward-only-skill-vault-migration-series.md:338-375`).

PASS. The migration target and registry both remain pending review, contain no approval marker, and do not claim self-approval. This file is the sole artifact created by this review. M2.03 remains gated until the coordinator records this review and the exact registry reconciliation.

## Final decision

`APPROVED_INDEPENDENT_LUNA_M2_02_R1_PASS`

M2.02 is accepted with zero P1/P2 findings. This approval is limited to the architecture/migration-series and versioned registry evidence reviewed here. It does not authorize writing migrations, changing the database, resolving TS2688, generating types, applying remote changes, implementing runtime behavior, using providers/credentials, deploying, or performing owner-pilot actions.

**Review artifact SHA-256:** computed after this final write and reported by the coordinator handoff.
