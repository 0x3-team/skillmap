# M2.15 — Generated database types checkpoint

Status: **APPROVED_LOCAL_ONLY**
Accepted: 2026-08-05
Branch / HEAD inspected: `main` / `cb062dd35de76cc3b40c1d004a13b50663cc1aa6`

## Verdict

M2.15 is accepted for local-only scope. The checked-in raw database types are byte-identical to the canonical Supabase generator output from the accepted local reset database for schemas `api,private`. Application consumers retain an explicit `api`-only runtime surface, and root, web, and worker typechecks all pass.

No P1 or P2 finding remains. No generated type was hand-edited after generation. No commit, push, deployment, remote Supabase action, provider action, production credential action, or owner-pilot action was performed.

## Final implementation

- `.gitea/workflows/ci.yml`
  - generates and compares the exact `api,private` raw type surface in CI.
- `apps/web/lib/supabase/database.types.ts`
  - is the byte-exact output of `supabase gen types typescript --local --schema api,private`, with only the generator's trailing blank line removed by the existing normalization command;
  - includes all 15 accepted Skill Vault private tables and the critical API/private RPC surface through M2.14.
- `apps/web/lib/supabase/database.runtime.types.ts`
  - excludes both generated schemas before rebuilding the documented application-facing `api` schema;
  - exports a compile-time assertion that the runtime database exposes exactly `api`, never `private`.
- `apps/web/scripts/check-ui-truth.mjs` and `test/release-truth-consistency.mjs`
  - freeze the raw/private versus runtime/api-only boundary and the worker typecheck gate.
- `apps/worker/package.json`, `apps/worker/tsconfig.json`, and `apps/worker/src/claim-lease.mjs`
  - add a real `checkJs` worker typecheck without changing runtime behavior;
  - add only the JSDoc needed for the existing claim/lease boundary to typecheck.
- `docs/operations/free-public-alpha-runbook.md`, `docs/operations/hosted-alpha-deploy.md`, and `apps/worker/README.md`
  - document the same local/linked `api,private` parity command and the application-private exclusion.
- Hosted security-definer allowlists were updated to include the already accepted M2.13 export RPC and the truthful 22-function surface found by the full pgTAP run.

## Canonical generation and surface evidence

Exact generation/parity command:

```sh
apps/web/node_modules/.bin/supabase gen types typescript --local --schema api,private \
  | sed -e '${/^$/d;}' > "$tmp_types"
cmp -s "$tmp_types" apps/web/lib/supabase/database.types.ts
```

- Byte-exact comparison: **PASS**.
- Raw generated type SHA-256: `57788bbd5cdff1ddff6f3ba61b43391dbb7c58bfb9eed808cc5650a5b6b83625`.
- Raw generated type size: `142161` bytes.
- Accepted Skill Vault private table surface: **PASS, 15/15 present**.
- Critical API/private function surface: **PASS, 15/15 present**.
- Supabase CLI documentation check: Context7 was unavailable in this session; pinned local Supabase CLI `2.110.0` help confirmed that `--schema` accepts a comma-separated schema list.

## Independent acceptance evidence

All decisive checks ran against the final local candidate.

- Clean local database reset through M2.14: **PASS**.
- Database lint at warning level: **PASS**, zero findings.
- Full database pgTAP suite after allowlist repair: **PASS, 23 files / 1558 tests**.
- Root TypeScript check: **PASS**.
- Web TypeScript check: **PASS**.
- Worker `checkJs` TypeScript check: **PASS**.
- Generated contract parity: **PASS**.
- Root contract suite: **PASS, 37/37**.
- Web UI truth contract: **PASS**.
- Release-truth consistency: **PASS, 7/7**.
- Web fixture suite and hosted boundaries: **PASS, 42/42 hosted-boundary tests**.
- Worker JavaScript syntax checks: **PASS**.
- Scoped web ESLint: **PASS**.
- `git diff --check`: **PASS**.

The first full pgTAP pass correctly rejected four stale security-definer allowlist assertions. The assertions were reconciled with the accepted M2.13 export surface, and the complete database suite then passed. Tests were not weakened or skipped.

## Final hashes

- raw generated types: `57788bbd5cdff1ddff6f3ba61b43391dbb7c58bfb9eed808cc5650a5b6b83625`
- runtime database types: `118a04d4beddfe695d1ae3306358f9e82d704eeb9945df65e4b24adf0039ddde`
- worker package: `2410544de69113545f602c60947377f7bb8faf1120c555e2700564ca9d3dbee6`
- worker TypeScript configuration: `82efbcf0dce613799eb3e74a9ac8641a9b4ee1d1aac676345d04e93dd2fc8f1a`
- worker claim/lease source: `1d56f4baa9d2a505ba6095b82f7903ee88e757f9fd918c5ac8795483557ddd85`
- CI workflow: `bbd2558063578690c14d206d3cedd8c66cfe40dfb8f8da352d21d84f26f89118`

## Visible DeepSeek and coordinator receipt

The visible Claude Code worker used `deepseek-v4-flash-0731` in terminal `term_fdf2ca88-aace-4c86-9142-773142dcbd69`; its reasoning setting was not exposed.

- The worker produced the canonical raw type generation, runtime-private exclusion, CI parity update, worker typecheck gate, and initial validation receipt.
- Its attempt to continue past the time cap and stop the local Supabase stack was rejected as coordinator behavior. The pane was closed, the stack was restored, and the exact clean reset and independent validation were rerun.
- The coordinator removed an unrelated JSDoc edit, added compile-time/runtime truth assertions, updated operator documentation, repaired the pgTAP allowlist drift exposed by the full suite, and completed final acceptance.

The strict required Luna High implementation identity was unavailable. The strict phase route therefore remains blocked with the user's previously authorized visible-DeepSeek/direct-repair exception; no claim is made that Luna High ran.

## Efficiency retrospective

- Generator-owned work should stop immediately after the byte-exact file, wrapper boundary, and narrow consumer fixes are written; deterministic local commands are cheaper and more reliable for the acceptance pass.
- One complete pgTAP run remained necessary because it exposed real cross-phase allowlist drift that focused type checks could not detect.
- M2.16 cannot reuse this local loop: it is a coordinator-owned remote action requiring an exact clean commit, explicit remote authorization, linked project identity, backup receipt, and disposable authenticated verification identities.

## Phase state

- M2.15: **current and APPROVED_LOCAL_ONLY**.
- M2.16: **eligible by predecessor sequence but BLOCKED_REMOTE_AUTHORIZATION; not started**.
- Commit/push/deploy/live/provider/credential/owner-pilot work: **not performed**.
