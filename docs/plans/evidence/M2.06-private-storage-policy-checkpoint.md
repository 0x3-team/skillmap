# M2.06 — Private Storage Policy Checkpoint

**Status:** `APPROVED_LOCAL_ONLY`
**Scope:** M2.06 only; no M2.07 work, live project action, deployment, external Storage API call, provider action, credential action, or owner-pilot action occurred.

## Implemented candidate

- `supabase/migrations/20260727060500_skill_vault_private_storage.sql`
  - SHA-256: `60530f993603e61a4da011b4bc59c2ab7e04142fc93d7e232a3fcee67dec5dc8`.
  - private `skill-vault-private` bucket, immutable exact object/file binding trigger, owner-scoped Storage policies, five-minute exact-object capability preparation, and idempotent incomplete-upload cleanup queue.
  - does not create indexes on vendor-owned `storage.objects`; the local reset proved that the migration role is not its owner. The object policy remains exact-owner scoped and the immutable binding trigger remains authoritative.
- `supabase/tests/skill_vault_storage.test.sql`
  - SHA-256: `7b37ad6ec5dd48ef7b7857c8e22055228af8120b5f1464c1f5c8345145ac55bc`.
  - 39 pgTAP assertions covering the private bucket contract and policies; two-account list/read/write/update/delete isolation; anonymous listing denial; malformed keys; size, media type, and raw-path metadata denial; short-lived exact-object capability preparation; cleanup idempotency, bounds, claim, completion, and rollback-only fixture cleanup. Direct SQL deletion is correctly rejected by the vendor `storage.protect_delete()` guard, so delete authorization is verified through the exact short-lived capability preparation and owner-scoped policy rather than bypassing the Storage API boundary.

## Validation truth at 2026-08-04T13:18:01Z

- `git diff --check --no-index /dev/null` for both M2.06 SQL files: `PASS`.
- pgTAP plan declaration matches 39 assertion calls: `PASS`.
- `npx --yes supabase db reset --local`: `PASS` after removal of the prohibited vendor-table index and cleanup enqueue ambiguity repair.
- `npx --yes supabase db lint --local --level warning`: `PASS`, zero schema errors/warnings.
- `npx --yes supabase test db supabase/tests/skill_vault_storage.test.sql --local`: `PASS`, 39/39.
- M2.05 regression: `supabase/tests/skill_vault_files.test.sql`: `PASS`, 74/74.
- M2.04 regression: `supabase/tests/skill_vault_versions_activation.test.sql`: `PASS`, 102/102.
- Catalog regression: `supabase/tests/hosted_catalog_rls.test.sql`: `PASS`, 97/97.
- M1.03 manifest vectors: `node --test test/m1-03-managed-manifest-vectors.mjs`: `PASS`, 3/3.
- Post-test local residue query: `cleanup=0`, `objects=0`, `policies=4`.
- `npm run typecheck` remains an unrelated workspace prerequisite failure on missing `node 2` type definitions; this task did not alter TypeScript or dependency configuration.

## Required continuation

The independent M2.06 migration/Supabase review accepted this candidate after rerunning the local gate and inspecting local ACL, policy, and trigger catalogs. Its findings are non-blocking:

1. **P2 — follow-up coverage:** direct `DELETE FROM storage.objects` is deliberately rejected by vendor `storage.protect_delete()`, so the two-account delete denial is currently proven by the owner-scoped delete policy plus the exact delete-capability preparation. Add an actual Storage API delete-denial integration test only when that caller exists; do not bypass the vendor boundary with table SQL.
2. **P3 — performance observation:** the vendor-owned `storage.objects` table has no migration-safe bucket-plus-owner index available to this migration role. Do not add one by assuming ownership; reassess it in a reviewed Storage/vendor performance path.

M2.06 is accepted for its local-only scope. M2.07 remains a separate, undispatched task and must not start from this checkpoint without its own authorization.
