# M2.14 — Account deletion checkpoint

Status: **APPROVED_LOCAL_ONLY**
Accepted: 2026-08-05
Branch / HEAD inspected: `main` / `cb062dd35de76cc3b40c1d004a13b50663cc1aa6`

## Verdict

M2.14 is accepted for local-only scope. The final candidate keeps the existing no-argument self-deletion contract, atomically removes the authenticated caller's private relational authority, queues exact private-object cleanup jobs, retains only a bounded proof receipt, and completes that receipt only through the frozen M1.11 13-owner acknowledgement sequence.

No P1 or P2 finding remains. No commit, push, deployment, remote Supabase action, provider action, production credential action, or owner-pilot action was performed. No authenticated browser or live deletion is claimed.

## Final implementation

- `supabase/migrations/20260727061300_skill_vault_account_deletion.sql`
  - adds FORCE-RLS deletion receipts and exact-object cleanup jobs with zero browser/service table grants;
  - preserves `api.delete_my_account()` as a no-argument authenticated boolean compatibility wrapper;
  - derives the caller only from verified auth state, takes the account advisory lock, snapshots exact object keys, and purges devices/tokens, import state, route state, managed files/releases/versions/skills, and the auth identity;
  - permits immutable route-row deletion only inside the ungranted barrier for the transaction-local caller coordinate; setting that coordinate alone grants no table authority;
  - exposes four bounded `deletion_adapter` worker commands only to `service_role`, while `service_role` retains no `private` schema usage or table grants;
  - requires physical object absence before a job can become `COMPLETED`, uses a closed failure vocabulary and bounded retry, and never claims blob completion from the account RPC;
  - validates the exact ordered 13-owner proof, exact count buckets and statuses, proof digest shape, completion window, and 30-day proof/backup bounds.
- `supabase/tests/skill_vault_account_deletion.test.sql`
  - exact 92-assertion destructive pgTAP plan with `finish()` and rollback;
  - covers self-only authority, missing/unknown/banned callers, privilege posture, full owned-domain deletion, other-account and public-catalog preservation, physical residue, absence-gated completion, failure replay, retry, and exact 13-owner completion.
- `apps/web/tests/skill-vault-account-deletion.test.mjs`
  - seven focused repository-standard source-contract checks for confirmation ordering, verified claims, no target account argument, local sign-out, exact boolean success, hardened short-lived flash cookie, and truthful asynchronous cleanup semantics.
- Hosted security-definer allowlists now include the accepted M2.13 `api.export_my_managed_skill_vault()` surface and its exact 22-function count; this closes the regression detected during M2.14 acceptance.

## Independent acceptance evidence

All decisive commands ran against the final clean local candidate.

- Clean local reset through `20260727061300_skill_vault_account_deletion.sql`: **PASS**.
- Database lint at warning level: **PASS**, zero findings.
- M2.14 focused pgTAP through the Supabase pgTAP runner: **PASS, 92/92**.
- All Skill Vault pgTAP regressions: **PASS** — export `75/75`, device/import RLS `68/68`, devices `74/74`, files `74/74`, import sessions `80/80`, managed RLS `132/132`, managed skills `57/57`, route records `74/74`, route RLS `65/65`, storage `39/39`, and versions/activation `102/102`.
- Hosted publication plus catalog-RLS regressions: **PASS, 176/176**.
- Account-deletion web contract: **PASS, 7/7**.
- Existing hosted-boundary suite: **PASS, 42/42**.
- Web TypeScript check: **PASS**.
- Scoped action/test ESLint: **PASS**.
- `git diff --check`: **PASS**.
- Focused receipt/job residue after rollback: `0|0`.

Effective privilege probe after the final reset:

- deletion adapter functions: `4`.
- anon/authenticated adapter execute: `0`.
- `service_role` private-schema usage: `false`.
- browser/service direct receipt/job grants: `0`.
- authenticated `api.delete_my_account()` execute: `true`; anon/service execute: `false|false`.

## Final hashes

- migration SHA-256: `7766fa7f4dc7e442c3f9c617a5206491347a8e4ededc0de9f511fb675ead814d`
- focused pgTAP SHA-256: `542f9dfe9361c1786c002c3aabcf3e930f37c44f8b22346c782a649f776a66b8`
- web contract test SHA-256: `90d2f64a726f6845aa0e8e9b6ab60e16e67d48e6d73ead5ade5210f852a18c2f`

## Visible DeepSeek and coordinator receipt

The visible Claude Code work used `deepseek-v4-flash-0731`.

- Initial migration/test panes `term_2861e8bf-6d1d-4165-9795-aaeece596b37` and `term_3a065415-b9ea-4904-ac58-a391c67b7e7f` produced incomplete drafts.
- Repair pane `term_abb190b0-1bf7-43f9-a2aa-2f36f872d207` produced no useful write within the cap and was closed.
- Final test pane `term_05155b26-61a4-46da-8845-654d035a53dd` was rejected and stopped after introducing structural SQL corruption.
- The coordinator kept the useful migration foundation, rebuilt the destructive test deterministically, found and repaired the immutable-route deletion defect, added the scoped route-delete authorization proof, closed the hosted allowlist regression, and ran the final validation matrix.

The strict required Luna High implementation identity was unavailable. The strict phase route therefore remains blocked with the user's previously authorized visible-DeepSeek/direct-repair exception; no claim is made that Luna High ran.

## Efficiency retrospective

- The external-model loop was stopped as soon as its repair quality fell below the cost of a deterministic local fix; this avoided another large rewrite.
- The most efficient acceptance loop was: one clean reset, one executable destructive fixture, repair only proven runtime defects, then run the affected regression fan-out in one bounded batch.
- Static source inspection was used only for the existing Next server-action boundary; database behavior was verified through real local transactions and pgTAP.
- M2.15 should use the existing generator scripts as the authority, generate once from the accepted reset database, and compare bytes before spending tokens on consumer-specific debugging.

## Phase state

- M2.14: **current and APPROVED_LOCAL_ONLY**.
- M2.15: **eligible, not started**.
- M2.16: **gated and remote-only; not authorized by this checkpoint**.
- Commit/push/deploy/live/provider/credential/owner-pilot work: **not performed**.
