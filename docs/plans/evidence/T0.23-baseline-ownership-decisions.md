# T0.23 Baseline ownership decisions

## Decision

**ACCEPTED.** The 11 modified Chromium/Linux PNGs and the accompanying
`manifest.json` entry are the canonical visual baseline set for this candidate.
This decision accepts the existing source-plus-baseline pair; it does not run
an update command or silently overwrite any baseline.

Decision time: `2026-07-23T22:41:47Z`
Evidence level: local, isolated pinned-renderer reproduction plus coordinator
visual review

## Decision basis

- T0.21 mapped every PNG to its exact capture owner. It found one direct source
  change for the onboarding handoff and no matching product-source change for
  the other ten captures.
- T0.22 reproduced every current PNG exactly in
  `mcr.microsoft.com/playwright:v1.61.1-noble` with Chromium
  `149.0.7827.0`: 11 pass, zero visual diffs, zero changed pixels, and zero
  generated diff artifacts.
- T0.23-A-R1 compared each current PNG with its `HEAD` predecessor using the
  repository Pixelmatch settings and an exact `diffMask` geometry pass.
- The coordinator reviewed side-by-side contact sheets for all 11 old/current
  pairs. No clipped content, overlap, missing control, unexpected state,
  misleading text, or workflow regression was found. The visible onboarding
  reflow is consistent with its mapped CSS change; the remaining changes are
  accepted as pinned-renderer baseline normalization, not as proof that one
  browser-version field alone caused every changed pixel.

## Per-artifact owner decisions

| Artifact | Capture owner | Measured change from `HEAD` | Decision | Recorded rationale |
| --- | --- | ---: | --- | --- |
| `activity-feedback-backlog.png` | `assertFeedbackBacklog` | 35 pixels | ACCEPT | Current capture reproduces exactly under the pinned renderer; the sparse localized delta has no visible product regression. |
| `eval-review-editor.png` | `exerciseEvalReviewImport` | 71,751 pixels | ACCEPT | Current capture reproduces exactly; broad raster/layout normalization is visually coherent and preserves the full editor workflow. |
| `onboarding-curation-handoff.png` | `assertCurationHandoff` | 9,304 pixels | ACCEPT | The changed onboarding CSS directly owns the reflow, and the resulting wrapped controls are legible, contained, and reproducible. |
| `overview-ready.png` | `exerciseLocalApp` | 35 pixels | ACCEPT | Current capture reproduces exactly; the sparse localized delta has no visible product regression. |
| `policy-dry-run.png` | `exercisePolicyPreview` | 35 pixels | ACCEPT | Current capture reproduces exactly; the sparse localized delta has no visible product regression. |
| `policy-proposal-ready.png` | `exercisePolicyProposalHold` | 24,695 pixels | ACCEPT | Current capture reproduces exactly; broader raster/layout normalization remains visually coherent and preserves the proposal workflow. |
| `recovery-required.png` | `exerciseDerivedRecovery` | 13,227 pixels | ACCEPT | Current capture reproduces exactly; the recovery state remains complete, readable, and operationally unchanged. |
| `route-recommendation.png` | `exerciseLocalApp` | 96 pixels | ACCEPT | Current capture reproduces exactly; its sparse, distributed delta has no visible product regression. |
| `settings-diagnostics.png` | `exerciseSettingsDiagnostics` | 247 pixels | ACCEPT | Current capture reproduces exactly; the localized delta does not alter diagnostics content or controls. |
| `source-upstream-diff.png` | `exerciseSourceAdoptionAndDiff` | 28,236 pixels | ACCEPT | Current capture reproduces exactly; the source-diff content and actions remain complete and readable. |
| `trace-detail.png` | `assertTracePermalink` | 24,708 pixels | ACCEPT | Current capture reproduces exactly; trace content, hierarchy, and permalink workflow remain intact. |
| `manifest.json` `browserVersion` | `createVisualGate` renderer contract | `149.0.7827.55` → `149.0.7827.0` | ACCEPT | The new value is the exact browser version observed in the pinned T0.22 image and therefore accurately records the accepted renderer. |

## Reconciliation

- PNG decisions recorded: `11 / 11`
- Manifest-entry decisions recorded: `1 / 1`
- Accepted: `12`
- Rejected: `0`
- Assigned UI corrections: `0`
- Unexplained candidate baselines remaining: `0`

The accepted ownership for the ten PNGs without a product-source mapping is
their named capture owner together with the pinned renderer contract. This is
an explicit candidate decision based on deterministic reproduction and visual
review; it does not rewrite T0.21's narrower source-attribution finding.

## T0.30 handoff

Run the normal visual gate without any update flag as part of the coherent
candidate validation ladder. Any new diff after this decision is a new
candidate change and requires a separate owner decision.
