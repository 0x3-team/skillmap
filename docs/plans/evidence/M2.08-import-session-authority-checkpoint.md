# M2.08 — Import-Session Authority Checkpoint

**Status:** `APPROVED_LOCAL_ONLY`
**Scope:** M2.08 only (resumable import-session and accepted file-receipt authority). No live/provider action, no deployment, no credential action, no commit/push, and no owner-pilot action occurred. M2.09 is eligible but not started; M2.10 and later remain sequentially gated.
**Acceptance:** The DeepSeek candidate passed binding Codex review after three repair rounds. M2.08 is accepted for its local-only scope with zero remaining P1/P2 findings.

## Independent-review receipts

- No external subagent lane was spawned inside Claude Code. Its parent DeepSeek session completed the implementation and self-review; the separate binding Codex receipt is recorded below.
- Binding Codex review independently inspected the repaired migration, pgTAP coverage, evidence, and append-only ledger; reran clean reset, private-schema lint, focused 80/80, predecessor regressions (74/5/39/74/102/97), M1.03 vectors 3/3, residue, diff, and hash checks; and found zero remaining M2.08 P1/P2 issues. Runtime model identity and Fast status were not exposed to the reviewer.

## Implemented candidate (post-mandatory-repair)

- `supabase/migrations/20260727060700_skill_vault_import_sessions.sql`
  - SHA-256: `22955d0aae7fec6a67e6e5fe0645b00fd92808602f40ee51d6574eabce51c8d1`.
  - Creates `private.import_sessions` (`imp_` public ID, account/device/skill/version binding, manifest identity/digests + schema version, expected/accepted counts+bytes, scoped `(account_id,device_id,idempotency_key)` idempotency, state, expiry, terminal `verified_at`/`verification_digest`, revision) and `private.import_file_receipts` (repeated account/device/session/file binding, accepted bytes/digest, ordinal, timestamp, full composite FKs incl. device_id). RLS enabled and FORCEd with zero policies; revokes all privileges from `public, anon, authenticated, service_role, skillmap_vault_definer` (including `import_session_has_exact_parity(uuid,uuid)` and `import_session_verification_digest(uuid,uuid)`). Provides ungranted `begin`/`resume`/`accept`/`finalize`/`expire` + the exact-parity and deterministic-digest helpers; M2.11 owns grants/RLS.
  - Repairs: (1) expected count/byte are bound to the authoritative `managed_skill_files` set of the bound version (not caller-supplied); (2) the `verified` transition is unforgeable — a direct UPDATE must carry the EXACT deterministic verification digest derived from the accepted receipt set (single shared helper, no drift), plus exact parity and schema/manifest/content identity parity, so an all-zero fabricated digest cannot forge verified; (3) schema-version parity at begin + idempotency identity; (4) parity explicitly rejects gapped ordinals (requires the authoritative receipt ordinal set to be exactly the contiguous 0..N-1).
- `supabase/tests/skill_vault_import_sessions.test.sql`
  - SHA-256: `4fef4dea3a56d65cd7ba66d05ab47650e497d060cde91bc90a3393a51d2838b1`.
  - 80 pgTAP assertions: forged-verified-digest defense, gapped-ordinal {2,5} finalize rejection, partial subset finalize rejection, schema-version parity, over-byte at the real reachable accept boundary, device isolation, foreign account/device/skill denial, expired/revoked device denial, expiry, resume isolation, terminal immutability, and a dedicated deleted-authoritative-file case proving session-row parity: after a 1/3 session is accepted and its authoritative file is deleted (receipt cascades), the session counter stays 1/3, the parity helper is false, and finalize raises 22023 keeping the session in_progress.

## Validation truth (post-repair, clean sequential run)

- `npx --yes supabase db reset --local`: `PASS` (clean bootstrap through M2.08).
- `npx --yes supabase db lint --local --schema private --level warning`: `PASS`, zero errors/warnings.
- `docker exec -i supabase_db_skillmap psql -U postgres -d postgres -v ON_ERROR_STOP=1 < supabase/tests/skill_vault_import_sessions.test.sql`: `PASS` (all assertions, then rollback).
- `npx --yes supabase test db supabase/tests/skill_vault_import_sessions.test.sql --local`: `PASS`, `Tests=80`.
- Predecessor suites:
  - `supabase/tests/skill_vault_devices.test.sql`: `PASS`, 74.
  - `supabase/tests/m2.07_credential_digest_contract_vectors.sql`: `PASS`, 5.
  - `supabase/tests/skill_vault_storage.test.sql`: `PASS`, 39.
  - `supabase/tests/skill_vault_files.test.sql`: `PASS`, 74.
  - `supabase/tests/skill_vault_versions_activation.test.sql`: `PASS`, 102.
  - `supabase/tests/hosted_catalog_rls.test.sql`: `PASS`, 97.
  - `supabase/tests/skill_vault_managed_skills.test.sql`: `FAIL` (2/57) — pre-existing M2.03-test vs M2.04-migration mismatch (asserts `managed_skills` has exactly the pre-M2.04 columns; M2.04 adds `active_release_id`/`activation_revision`). Unrelated to M2.08; M2.08 does not touch `managed_skills`.
- `node --test test/m1-03-managed-manifest-vectors.mjs`: `PASS`, 3 tests.
- Residue (post-run): `import_sessions=0`, `import_file_receipts=0`.
- Scoped whitespace/diff (`git diff --check` incl. new files): `PASS`.
- Forbidden-column/canary scan: no raw/token/prompt/body/path/secret-bearing columns; broad-grant scan: `NONE`.
- Ledger JSONL validity: `PASS` (all lines parse).

## Adversarial classes probed

- `direct_verified_manufacture`: a bare UPDATE to `verified` with an all-zero fabricated digest, with exact parity already satisfied, is rejected (`verified transition requires the exact deterministic verification digest`) and the session stays in_progress; normal finalize then succeeds with the deterministic digest.
- `gapped_ordinals`: a version with authoritative ordinals {2,5} (count 2, bytes 7) is rejected by parity and finalize (`import session does not match the authoritative version file set`) despite matching count/bytes.
- `partial_multi_file_finalize`: two-file version finalize after accepting only one file is rejected.
- `over_byte_at_reachable_boundary`: begin a version-103 session (one 3-byte file), accept it, add a second same-version 3-byte file at ordinal 1, accept it → rejected (SQLSTATE 22023, `import byte total would exceed the session expectation`) with counters held at 1/3.
- `deleted_authoritative_file_session_row_parity`: begin a 1/3 session, accept the sole file, DELETE the authoritative `managed_skill_file` so its receipt cascades (M2.05 permits DELETE). The session still records accepted 1/3 while both the authoritative and receipt aggregates are 0/0. The parity helper is now false (session-row equality) and finalize raises 22023 (`import session does not match the authoritative version file set`), leaving the session in_progress — an empty set can never be finalized.
- `wrong_schema_version` (9.9 on a 1.0 version rejected), `cross_account_authority`, `device_isolation`, `expiry`, `terminal_immutability`, `replay_idempotency`, `privilege_escalation`.

## P3 residuals (local-only, owner tracked)

- M2.11 owns device/import RLS; M2.08 keeps tables forced-RLS with zero policies as designed.
- M2.15 owns database type regeneration; M2.08 adds two new private tables and the parity/verification-digest helpers that must appear in regenerated types.
- The pre-existing M2.03 managed-skills test vs M2.04 migration mismatch is a separate defect outside M2.08 scope and is reported as such, not hidden.

## Continuation

This checkpoint records binding local-only M2.08 acceptance. M2.09 is now eligible but remains not started; no later task was dispatched. The earlier stale hashes/PASS claims remain immutable history and are superseded by the corrective and acceptance ledger rows; ledger row 269 was not edited.
