# T0.21 Visual Baseline Source Map

## Scope and method

This is a source-map audit only. It does not run a visual gate, regenerate an
image, or decide whether any baseline should be accepted.

The modified baseline set was enumerated with Git's modified-path filter for
`apps/web/tests/visual-baselines/local-app/chromium-linux/*.png`: 11 files. Each
path appears once in the table below.

The local-app runner measures and serves `assets/local-app/v1` (see
`apps/web/scripts/local-app-browser.mjs:88` and `src/commands/dashboard.ts:13`).
The changed Next layout and hosted metadata files are therefore not render
sources for these local-app captures. The only changed local-app stylesheet
hunk is `assets/local-app/v1/app.css:231-233`; the full CSS diff shows three
selectors, all scoped below `.onboarding-steps`, and no changed global
selector. The two `local-app-browser.mjs` changes are a workspace `realpath`
assertion and a WebKit navigation-abort classification; neither changes page
markup, stylesheet input, or the capture helper.

## PNG-to-source mapping

| Modified baseline | Exact capture owner | Changed source evidence | Status |
| --- | --- | --- | --- |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/activity-feedback-backlog.png` | `assertFeedbackBacklog` at `apps/web/scripts/local-app-browser.mjs:1049`, capture at `:1071` | **UNEXPLAINED.** The changed selectors are restricted to `.onboarding-steps`; this capture is the feedback backlog state, not an onboarding step list. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/eval-review-editor.png` | `exerciseEvalReviewImport` at `apps/web/scripts/local-app-browser.mjs:1169`, capture at `:1208` | **UNEXPLAINED.** No changed evaluator render component or matching selector is in the diff; the onboarding-only CSS does not target this editor. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/onboarding-curation-handoff.png` | `assertCurationHandoff` at `apps/web/scripts/local-app-browser.mjs:1121`, capture at `:1144` | `assets/local-app/v1/modules/views/onboarding.js:48` renders `ol.onboarding-steps`, each first-child text `span`, and each `.step-action`; the changed selectors at `assets/local-app/v1/app.css:231-233` alter those exact elements' shrink, flex, and pill wrapping behavior. | Direct source mapping |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/overview-ready.png` | `exerciseLocalApp` at `apps/web/scripts/local-app-browser.mjs:395`, capture at `:540` | **UNEXPLAINED.** No changed Overview component or selector matches this capture; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/policy-dry-run.png` | `exercisePolicyPreview` at `apps/web/scripts/local-app-browser.mjs:879`, capture at `:943` | **UNEXPLAINED.** No changed policy preview component or matching selector is in the diff; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/policy-proposal-ready.png` | `exercisePolicyProposalHold` at `apps/web/scripts/local-app-browser.mjs:947`, capture at `:991` | **UNEXPLAINED.** No changed policy proposal component or matching selector is in the diff; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/recovery-required.png` | `exerciseDerivedRecovery` at `apps/web/scripts/local-app-browser.mjs:282`, capture at `:303` | **UNEXPLAINED.** No changed recovery render component or selector matches this capture; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/route-recommendation.png` | `exerciseLocalApp` at `apps/web/scripts/local-app-browser.mjs:395`, capture at `:594` | **UNEXPLAINED.** No changed Route Lab component or selector matches this capture; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/settings-diagnostics.png` | `exerciseSettingsDiagnostics` at `apps/web/scripts/local-app-browser.mjs:1075`, capture at `:1089` | **UNEXPLAINED.** No changed settings component or selector matches this capture; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/source-upstream-diff.png` | `exerciseSourceAdoptionAndDiff` at `apps/web/scripts/local-app-browser.mjs:1234`, capture at `:1352` | **UNEXPLAINED.** No changed sources component or selector matches this capture; the stylesheet diff is onboarding-only. | Unexplained |
| `apps/web/tests/visual-baselines/local-app/chromium-linux/trace-detail.png` | `assertTracePermalink` at `apps/web/scripts/local-app-browser.mjs:1977`, capture at `:2055` | **UNEXPLAINED.** No changed trace-detail component or selector matches this capture; the stylesheet diff is onboarding-only. | Unexplained |

## Manifest reconciliation

`apps/web/tests/visual-baselines/local-app/chromium-linux/manifest.json` changes
only `browserVersion`, from `149.0.7827.55` to `149.0.7827.0`; it does not add,
remove, or associate individual PNG names.

`createVisualGate` constructs and compares the capture environment at
`apps/web/scripts/local-app-qa.mjs:111-142`, then reads or writes a baseline by
capture name at `:171-205`. That makes the manifest a reproducibility contract
and an environment-change hypothesis, not evidence that the browser-version
change caused any particular PNG delta. It is not used as visual proof in the
mapping above.

## Totals and required follow-up

- Modified PNGs from Git: 11
- Direct source mappings: 1
- Unexplained: 10
- Total: 1 + 10 = 11

T0.22 must reproduce the captures with the relevant renderer and environment
inputs to resolve the browser or other environment hypothesis for the ten
unexplained baselines, and to quantify the onboarding mapping. This report
makes no baseline acceptance decision.
