# M2.11 — Device/import RLS checkpoint

Status: **APPROVED_LOCAL_ONLY**
Accepted: 2026-08-05
Branch / HEAD inspected: `main` / `cb062dd35de76cc3b40c1d004a13b50663cc1aa6`

## Verdict

M2.11 is accepted for local-only scope. No P1 or P2 finding remains in the reviewed migration, reachable-role matrix, or regression surface. M2.12 is eligible but was not started by this checkpoint; M2.13 and later remain sequentially gated.

This checkpoint does not claim a commit, push, deployment, hosted migration, live/provider action, production credential action, or owner pilot.

## Accepted implementation

- `supabase/migrations/20260727061000_skill_vault_device_import_rls.sql`
  - forces RLS on devices, tokens, import sessions, and receipts;
  - retains zero direct private-table grants for `anon`, `authenticated`, and `service_role`;
  - exposes bounded owner device/import views with no internal UUID, account, verifier, key-version, scope, or digest columns;
  - exposes authenticated-only registration, revocation, and credential-reset functions deriving ownership from `auth.uid()`;
  - establishes a dedicated, non-PostgREST `device_adapter` schema whose only application grantee is `service_role`;
  - exposes exactly seven service adapter wrappers and leaves `service_role` without `private` schema usage;
  - derives device identity from a live credential verifier and validates account, device revision, token generation, scope, expiry, revocation, and replacement before import access;
  - requires explicit future import expiry, enforces exact expiry-aware idempotency replay/conflict, and binds resume/accept/finalize to the derived device and session revision;
  - prevents a second simultaneously live token from being issued for a device.
- `supabase/tests/skill_vault_device_import_rls.test.sql`
  - exact pgTAP plan plus `select * from finish()`;
  - 68 reachable-role assertions across `anon`, `authenticated`, and `service_role`;
  - A/B isolation, projection redaction, grants, forced RLS, direct-DML denial, scope/account/revision/generation/expiry/replacement/revocation denial, exact replay/conflict, accept replay, session expiry, finalization, and zero-residue coverage.
- Forward-test posture only:
  - `supabase/tests/skill_vault_devices.test.sql` now expects the one M2.11 device owner-select policy.
  - `supabase/tests/skill_vault_import_sessions.test.sql` now expects the one M2.11 owner-select policy on sessions and receipts.
  - No predecessor migration or evidence file was edited.

## Independent acceptance evidence

All suites ran on the clean local candidate. After the final dedicated-schema hardening, the affected decisive suites M2.07, M2.08, and M2.10, the M2.11 focused suite, and database lint were rerun and remained green. The broader regressions listed below ran before that final schema-only hardening and were not rerun afterward because the change removed a service-role privilege from `private` and moved only the new M2.11 adapter wrappers into `device_adapter`.

- Clean local reset: **PASS**; all migrations through `20260727061000_skill_vault_device_import_rls.sql` applied.
- Private schema lint at warning level: **PASS**, zero errors/warnings; schemas included `api`, `device_adapter`, `private`, and `public`.
- M2.11 focused pgTAP: **PASS, 68/68**, exact plan and `finish()`.
- M2.03 managed skills: **PASS, 57/57**.
- M2.04 versions/releases/activation: **PASS, 102/102**.
- M2.05 files: **PASS, 74/74**.
- M2.06 storage: **PASS, 39/39**.
- M2.07 devices/tokens: **PASS, 74/74**.
- M2.07 credential vectors: **PASS, 5/5**.
- M2.08 import sessions: **PASS, 80/80**.
- M2.09 route records: **PASS, 74/74**.
- M2.10 managed RLS: **PASS, 132/132**.
- Hosted catalog: **PASS, 97/97**.
- Hosted submission authority: **PASS, 55/55**.
- Hosted operator publication: **PASS, 79/79**.
- M1.03 Node manifest vectors: **PASS, 3/3**.
- Scoped `git diff --check`: **PASS**.

Effective privilege probe after the final reset:

- `service_role` `USAGE` on `device_adapter`: `true`.
- `service_role` `USAGE` on `private`: `false`.
- service-executable functions in `device_adapter`: exactly `7`.
- browser-executable functions in `device_adapter`: `0`.
- service direct private-table privileges: `0`.

Focused fixture residue after rollback:

- users: `0`
- devices: `0`
- device tokens: `0`
- import sessions: `0`
- import receipts: `0`

## Accepted hashes

- migration: `0a1b2978277c2c2d74b79abef882a6ed12e60cda0a298525b3faf900bb10f0dd`
- focused test: `b7db7c52eb2420f0d365c9c6907c6f486a2b3a19dc8383a27de7f196c6a3c7fc`
- device predecessor test: `376128b7ccdf3506b814279562b36ccefb83797a5a4d0ae800a75adc1abccd69`
- import predecessor test: `d7f154b7160c7b23c2c2dbe83a134baa18e56b59fdf24a45788e355215e27255`

## Orchestration and review note

Visible Claude Code was launched with exact model `deepseek-v4-flash-0731`. Two implementation attempts and one focused test/evidence attempt were physically fenced after bounded windows because they consumed model tokens without writing any expected file. Dispatches: `ctx_c52bc3f80249`, `ctx_ba82b0581236`, and `ctx_979990afbaa9`.

The coordinator then used the authorized direct-fix path, implemented the narrow migration and test, repaired defects exposed by executable tests, and performed the acceptance review. The strict required Luna High implementation identity was unavailable, so the strict phase route remains blocked with the user's previously authorized DeepSeek/direct-repair exception; no claim is made that Luna High ran.

## Phase state

- M2.11: **current and APPROVED_LOCAL_ONLY**.
- M2.12: **eligible, not started**.
- M2.13 and later: **gated**.
- Commit/push/deploy/live/provider/credential/owner-pilot work: **not performed**.
